import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react-swc'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
})



// vite tool : use to mutiple things intall krte hai 
// not change this file

// package.json
// tell the versions of the packages 
// tell about the packages 
// by the package.json we can again install node Modules files by node i command

// package-lock.json